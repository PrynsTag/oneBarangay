"""File for user_profile config."""
from django.apps import AppConfig


class UserProfileConfig(AppConfig):
    """Class for user_profile config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "user_profile"
