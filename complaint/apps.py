"""File for complaint config."""
from django.apps import AppConfig


class BulkSchedConfig(AppConfig):
    """Class for complaint config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "complaint"
