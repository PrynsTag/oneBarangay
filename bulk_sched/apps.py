"""File for bulk_sched config."""
from django.apps import AppConfig


class BulkSchedConfig(AppConfig):
    """Class for bulk_sched config."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "bulk_sched"
