"""This is a file for App Configuration."""
from django.apps import AppConfig


class AppConfig(AppConfig):
    """A class for App Configuration."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "app"
